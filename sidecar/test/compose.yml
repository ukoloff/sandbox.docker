name: sidecar-test

services:
  node:
    build:
      dockerfile_inline: |
        FROM node:alpine
        RUN --mount=type=cache,target=/var/cache/apk apk add git
    command:
      - sleep
      - infinity
    stop_signal: SIGKILL
