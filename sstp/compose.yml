services:
  sstp:
    image: ukoloff/sstp
    privileged: true
    volumes:
    - /dev/ppp:/dev/ppp
    - /etc/ssl/uxm:/ssl
    ports:
    - 443:443
    command:
    - uv
    - run
    - sstpd
    - -f
    - /etc/sstpd.ini
