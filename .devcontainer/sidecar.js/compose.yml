name: sdcr-js

services:
  node:
    image: ukoloff/dev:js
    volumes:
      - type: volume
        source: repo
        target: /repo
      - type: tmpfs
        target: /var/cache/apk
    working_dir: /repo
    init: true
    command:
      - sleep
      - infinity
    depends_on:
      sidecar:
        condition: service_completed_successfully

  sidecar:
    image: ukoloff/sidecar:js
    volumes:
      - repo:/repo
    working_dir: /repo
    environment:
      SIDECAR_GIT_URL: ukoloff/sandbox.docker/sidecar?dev=1#sidecar

volumes:
  repo:
