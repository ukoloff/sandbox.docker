{
  "dockerComposeFile": "./compose.yml",
  "service": "node",
  "onCreateCommand": "git clone -b sidecar --depth 2 -n --filter=tree:0 https://github.com/ukoloff/sandbox.docker.git . \n git sparse-checkout set --no-cone /sidecar \n git checkout",
  // "updateContentCommand": "git sparse-checkout set --no-cone /sidecar",
  // "postCreateCommand": "git checkout",
  "workspaceFolder": "/repo",
  "customizations": {
    "vscode": {
      "extensions": [
        "mhutchie.git-graph",
        "EditorConfig.EditorConfig"
      ]
    }
  }
}
