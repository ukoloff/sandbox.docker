{
  "name": "sdcr.js",
  "dockerComposeFile": "./compose.yml",
  "service": "node",
  "onCreateCommand": "apk add git",
  // "updateContentCommand":
  "postCreateCommand": "git clone -b sidecar --depth 2 -n --filter=tree:0 https://github.com/ukoloff/sandbox.docker . ; git sparse-checkout set --no-cone /sidecar/ ; git checkout",
  "workspaceFolder": "/repo",
  "customizations": {
    "vscode": {
      "extensions": [
        "mhutchie.git-graph",
        "EditorConfig.EditorConfig"
      ]
    }
  }
}
