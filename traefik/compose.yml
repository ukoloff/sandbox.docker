name: traefik

services:
  itself:
    image: traefik:v3.5
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - etc:/etc/traefik
    ports:
      - 80:80
      - 8080:8080

volumes:
  etc:
